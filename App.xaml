<?xml version="1.0" encoding="UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:converters="clr-namespace:AetherVault.Converters"
             x:Class="AetherVault.App">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Converters -->
            <toolkit:InvertedBoolConverter x:Key="InvertedBool" />
            <converters:LegalityToColorConverter x:Key="LegalityToColor" />
            <converters:BoolToChevronConverter x:Key="BoolToChevronConverter" />

            <!-- Colors -->
            <Color x:Key="Primary">#6200EE</Color>
            <Color x:Key="PrimaryDark">#3700B3</Color>
            <Color x:Key="Accent">#03DAC5</Color>
            <Color x:Key="Background">#121212</Color>
            <Color x:Key="Surface">#1E1E1E</Color>
            <Color x:Key="CardBackground">#252525</Color>
            <Color x:Key="TextPrimary">#F0F0F0</Color>
            <Color x:Key="TextSecondary">#A0A0A0</Color>
            <Color x:Key="ErrorColor">#CF6679</Color>
            <Color x:Key="LegalGreen">#4CAF50</Color>
            <Color x:Key="BannedRed">#F44336</Color>
            <Color x:Key="RestrictedYellow">#FFC107</Color>
            <Color x:Key="RarityCommon">#C0C0C0</Color>
            <Color x:Key="RarityUncommon">#B0C4DE</Color>
            <Color x:Key="RarityRare">#FFD700</Color>
            <Color x:Key="RarityMythic">#FF8C00</Color>

            <!-- Styles -->
            <Style TargetType="Page" ApplyToDerivedTypes="True">
                <Setter Property="BackgroundColor" Value="{StaticResource Background}" />
            </Style>

            <Style TargetType="Shell" ApplyToDerivedTypes="True">
                <Setter Property="Shell.BackgroundColor" Value="{StaticResource Surface}" />
                <Setter Property="Shell.ForegroundColor" Value="{StaticResource TextPrimary}" />
                <Setter Property="Shell.TabBarBackgroundColor" Value="{StaticResource Surface}" />
                <Setter Property="Shell.TabBarForegroundColor" Value="{StaticResource Primary}" />
                <Setter Property="Shell.TabBarUnselectedColor" Value="{StaticResource TextSecondary}" />
                <Setter Property="Shell.TitleColor" Value="{StaticResource TextPrimary}" />
            </Style>

            <Style TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource TextPrimary}" />
                <Setter Property="FontSize">
                    <Setter.Value>
                        <OnIdiom x:TypeArguments="x:Double">
                            <OnIdiom.Phone>14</OnIdiom.Phone>
                            <OnIdiom.Tablet>18</OnIdiom.Tablet>
                            <OnIdiom.Desktop>18</OnIdiom.Desktop>
                        </OnIdiom>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="Entry">
                <Setter Property="TextColor" Value="{StaticResource TextPrimary}" />
                <Setter Property="PlaceholderColor" Value="{StaticResource TextSecondary}" />
                <Setter Property="BackgroundColor" Value="Transparent" />
                <Setter Property="FontSize">
                    <Setter.Value>
                        <OnIdiom x:TypeArguments="x:Double">
                            <OnIdiom.Phone>14</OnIdiom.Phone>
                            <OnIdiom.Tablet>18</OnIdiom.Tablet>
                            <OnIdiom.Desktop>18</OnIdiom.Desktop>
                        </OnIdiom>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="FontSize">
                    <Setter.Value>
                        <OnIdiom x:TypeArguments="x:Double">
                            <OnIdiom.Phone>14</OnIdiom.Phone>
                            <OnIdiom.Tablet>18</OnIdiom.Tablet>
                            <OnIdiom.Desktop>18</OnIdiom.Desktop>
                        </OnIdiom>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="FloatingActionButtonStyle" TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource Accent}" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="WidthRequest" Value="56" />
                <Setter Property="HeightRequest" Value="56" />
                <Setter Property="CornerRadius" Value="28" />
                <Setter Property="FontSize" Value="22" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="HorizontalOptions" Value="End" />
                <Setter Property="VerticalOptions" Value="End" />
                <Setter Property="Margin" Value="0,0,16,80" />
                <Setter Property="Shadow">
                    <Setter.Value>
                        <Shadow Brush="Black"
                                Offset="0,4"
                                Radius="10"
                                Opacity="0.45" />
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- UraniumUI ExpanderView Arrow -->
            <Style TargetType="Path" Class="ExpanderView.Arrow">
                <Setter Property="Fill" Value="{StaticResource TextPrimary}" />
            </Style>

            <Style x:Key="CardStyle" TargetType="Border">
                <Setter Property="BackgroundColor" Value="{StaticResource CardBackground}" />
                <Setter Property="Stroke" Value="Transparent" />
                <Setter Property="StrokeShape" Value="RoundRectangle 8" />
                <Setter Property="Padding" Value="10" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
