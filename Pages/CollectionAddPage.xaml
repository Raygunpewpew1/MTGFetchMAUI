<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:fa="clr-namespace:UraniumUI.Icons.FontAwesome;assembly=UraniumUI.Icons.FontAwesome"
             xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
             xmlns:controls="clr-namespace:AetherVault.Controls"
             x:Class="AetherVault.Pages.CollectionAddPage"
             BackgroundColor="{StaticResource Background}">

    <Border x:Name="SheetContainer"
            BackgroundColor="{StaticResource Surface}"
            StrokeThickness="0"
            StrokeShape="RoundRectangle 24"
            Padding="20,24,20,32"
            VerticalOptions="Center"
            HorizontalOptions="Center">
        <Border.Shadow>
            <Shadow Brush="Black" Offset="0,4" Radius="20" Opacity="0.4" />
        </Border.Shadow>

        <VerticalStackLayout Spacing="20" WidthRequest="300">
            <!-- Card Info Header -->
            <VerticalStackLayout Spacing="2">
                <Label x:Name="TitleLabel"
                       FontSize="20"
                       FontAttributes="Bold"
                       HorizontalTextAlignment="Center"
                       TextColor="{StaticResource TextPrimary}"
                       LineBreakMode="TailTruncation" />
                <Label x:Name="SetLabel"
                       FontSize="13"
                       HorizontalTextAlignment="Center"
                       TextColor="{StaticResource TextSecondary}" />
            </VerticalStackLayout>

            <!-- Quantity Selector -->
            <VerticalStackLayout Spacing="6">
                <controls:QuantitySelectorView x:Name="QuantitySelector"
                                               HeaderText="Quantity" />
                <Label x:Name="RemoveWarningLabel"
                       Text="(will remove from collection)"
                       FontSize="11"
                       TextColor="{StaticResource ErrorColor}"
                       HorizontalTextAlignment="Center"
                       IsVisible="False" />
            </VerticalStackLayout>

            <Label x:Name="CollectionInfoLabel"
                   FontSize="13"
                   TextColor="{StaticResource Accent}"
                   HorizontalTextAlignment="Center"
                   FontAttributes="Italic" />

            <!-- Finish Toggles -->
            <VerticalStackLayout Spacing="10">
                <material:CheckBox x:Name="FoilCheckBox"
                                   Text="Foil"
                                   Color="{StaticResource Primary}" />
                <material:CheckBox x:Name="EtchedCheckBox"
                                   Text="Etched"
                                   Color="{StaticResource Primary}" />
            </VerticalStackLayout>

            <!-- Actions -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="12" Margin="0,6,0,0">
                <Button Text="Cancel"
                        Clicked="OnCancelClicked"
                        HeightRequest="52"
                        StyleClass="OutlinedButton" />

                <Button Grid.Column="1"
                        x:Name="ConfirmBtn"
                        Text="Add to Collection"
                        Clicked="OnConfirmClicked"
                        HeightRequest="52"
                        FontAttributes="Bold"
                        StyleClass="FilledButton" />
            </Grid>
        </VerticalStackLayout>
    </Border>

</ContentPage>
