<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:skia="clr-namespace:SkiaSharp.Views.Maui.Controls;assembly=SkiaSharp.Views.Maui.Controls"
             xmlns:controls="clr-namespace:AetherVault.Controls"
             xmlns:vm="clr-namespace:AetherVault.ViewModels"
             xmlns:pages="clr-namespace:AetherVault.Pages"
             xmlns:fa="clr-namespace:UraniumUI.Icons.FontAwesome;assembly=UraniumUI.Icons.FontAwesome"
             x:Class="AetherVault.Pages.DeckDetailPage"
             Title="{Binding Deck.Name}"
             BackgroundColor="{StaticResource Background}"
             x:DataType="vm:DeckDetailViewModel">

    <Grid RowDefinitions="Auto,140,Auto,*,Auto">

        <!-- Row 0: Deck header card -->
        <Border Grid.Row="0"
                Style="{StaticResource CardStyle}"
                Margin="8,8,8,4">
            <Grid ColumnDefinitions="*,Auto"
                  ColumnSpacing="12">
                <VerticalStackLayout Spacing="4">
                    <Label Text="{Binding Deck.Name}"
                           FontSize="17"
                           FontAttributes="Bold"
                           TextColor="{StaticResource TextPrimary}"
                           LineBreakMode="TailTruncation" />
                    <Label Text="{Binding DeckFormat}"
                           FontSize="12"
                           TextColor="{StaticResource TextSecondary}" />
                </VerticalStackLayout>

                <Border Grid.Column="1"
                        BackgroundColor="{StaticResource Accent}"
                        Padding="10,5"
                        StrokeShape="RoundRectangle 14"
                        Stroke="Transparent"
                        VerticalOptions="Center">
                    <Label Text="{Binding TotalCardCount}"
                           TextColor="Black"
                           FontSize="14"
                           FontAttributes="Bold" />
                </Border>
            </Grid>
        </Border>

        <!-- Row 1: Commander Art Header (gradient placeholder, shows commander name) -->
        <skia:SKCanvasView Grid.Row="1"
                           x:Name="CommanderArtCanvas"
                           PaintSurface="OnCommanderArtPaint" />

        <!-- Row 2: Section Tab Bar -->
        <Grid Grid.Row="2"
              ColumnDefinitions="*,*,*,*"
              BackgroundColor="{StaticResource Surface}"
              HeightRequest="44">
            <!-- Commander Tab -->
            <Grid Grid.Column="0">
                <Grid.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding SelectCommanderCommand}" />
                </Grid.GestureRecognizers>
                <Label Text="Commander" FontSize="12" FontAttributes="{Binding Tab0Font}" TextColor="{Binding Tab0Color}" HorizontalOptions="Center" VerticalOptions="Center" />
                <BoxView HeightRequest="3" Color="{Binding Tab0Color}" Opacity="{Binding Tab0Indicator}" VerticalOptions="End" HorizontalOptions="Fill" />
            </Grid>
            <!-- Main Tab -->
            <Grid Grid.Column="1">
                <Grid.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding SelectMainCommand}" />
                </Grid.GestureRecognizers>
                <Label Text="Main" FontSize="12" FontAttributes="{Binding Tab1Font}" TextColor="{Binding Tab1Color}" HorizontalOptions="Center" VerticalOptions="Center" />
                <BoxView HeightRequest="3" Color="{Binding Tab1Color}" Opacity="{Binding Tab1Indicator}" VerticalOptions="End" HorizontalOptions="Fill" />
            </Grid>
            <!-- Sideboard Tab -->
            <Grid Grid.Column="2">
                <Grid.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding SelectSideboardCommand}" />
                </Grid.GestureRecognizers>
                <Label Text="Sideboard" FontSize="12" FontAttributes="{Binding Tab2Font}" TextColor="{Binding Tab2Color}" HorizontalOptions="Center" VerticalOptions="Center" />
                <BoxView HeightRequest="3" Color="{Binding Tab2Color}" Opacity="{Binding Tab2Indicator}" VerticalOptions="End" HorizontalOptions="Fill" />
            </Grid>
            <!-- Stats Tab -->
            <Grid Grid.Column="3">
                <Grid.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding SelectStatsCommand}" />
                </Grid.GestureRecognizers>
                <Label Text="Stats" FontSize="12" FontAttributes="{Binding Tab3Font}" TextColor="{Binding Tab3Color}" HorizontalOptions="Center" VerticalOptions="Center" />
                <BoxView HeightRequest="3" Color="{Binding Tab3Color}" Opacity="{Binding Tab3Indicator}" VerticalOptions="End" HorizontalOptions="Fill" />
            </Grid>
        </Grid>

        <!-- Row 3: Content Panels + FAB overlay -->
        <Grid Grid.Row="3">

            <!-- Last-added history chip -->
            <Border BackgroundColor="{StaticResource Surface}"
                    StrokeShape="RoundRectangle 18"
                    Padding="10,6"
                    Margin="12,8"
                    HorizontalOptions="Start"
                    VerticalOptions="Start"
                    IsVisible="{Binding HasLastAdded}">
                <HorizontalStackLayout Spacing="8" VerticalOptions="Center">
                    <Label Text="{Binding LastAddedSummaryText}"
                           FontSize="11"
                           TextColor="{StaticResource TextSecondary}"
                           VerticalOptions="Center" />
                    <Button Text="Undo"
                            Command="{Binding UndoLastAddedCommand}"
                            FontSize="11"
                            HeightRequest="26"
                            Padding="10,0"
                            BackgroundColor="{StaticResource Accent}"
                            TextColor="Black"
                            CornerRadius="13" />
                </HorizontalStackLayout>
            </Border>

            <!-- Commander Tab -->
            <ScrollView IsVisible="{Binding IsCommanderTab}" Padding="16,8">
                <VerticalStackLayout Spacing="12">
                    <!-- No commander placeholder -->
                    <VerticalStackLayout IsVisible="{Binding HasNoCommander}"
                                         VerticalOptions="Center"
                                         HorizontalOptions="Center"
                                         Spacing="8"
                                         Padding="0,40,0,0">
                        <Label Text="No commander set"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="{StaticResource TextPrimary}"
                               HorizontalOptions="Center" />
                        <Label Text="Tap + to search for a legendary creature"
                               FontSize="13"
                               TextColor="{StaticResource TextSecondary}"
                               HorizontalOptions="Center"
                               HorizontalTextAlignment="Center" />
                    </VerticalStackLayout>

                    <!-- Commander cards -->
                    <CollectionView ItemsSource="{Binding CommanderCards}"
                                    SelectionMode="None">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="vm:DeckCardDisplayItem">
                                <SwipeView>
                                    <SwipeView.RightItems>
                                        <SwipeItems>
                                            <SwipeItem Text="Remove"
                                                       BackgroundColor="#B00020"
                                                       Command="{Binding Source={RelativeSource AncestorType={x:Type pages:DeckDetailPage}}, Path=BindingContext.RemoveCardCommand}"
                                                       CommandParameter="{Binding .}" />
                                        </SwipeItems>
                                    </SwipeView.RightItems>
                                    <Border Style="{StaticResource CardStyle}"
                                            Margin="0,4">
                                        <Grid ColumnDefinitions="Auto,*,Auto,Auto"
                                              Padding="8,6"
                                              ColumnSpacing="8">
                                            <Border BackgroundColor="{StaticResource Accent}"
                                                    Padding="6,2"
                                                    StrokeShape="RoundRectangle 6"
                                                    Stroke="Transparent"
                                                    VerticalOptions="Center">
                                                <Label Text="{Binding Entity.Quantity}"
                                                       TextColor="Black"
                                                       FontSize="12"
                                                       FontAttributes="Bold" />
                                            </Border>
                                            <Label Grid.Column="1"
                                                   Text="{Binding DisplayName}"
                                                   TextColor="{StaticResource TextPrimary}"
                                                   FontSize="14"
                                                   VerticalOptions="Center" />
                                            <controls:ManaCostView Grid.Column="2"
                                                   ManaText="{Binding ManaCostText}"
                                                   SymbolSize="12"
                                                   VerticalOptions="Center" />
                                        </Grid>
                                    </Border>
                                </SwipeView>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </ScrollView>

            <!-- Main Deck Tab -->
            <CollectionView IsVisible="{Binding IsMainTab}"
                            ItemsSource="{Binding MainDeckGroups}"
                            IsGrouped="True"
                            SelectionMode="None">
                <CollectionView.GroupHeaderTemplate>
                    <DataTemplate x:DataType="vm:DeckCardGroup">
                        <Label Text="{Binding GroupName}"
                               FontSize="12"
                               FontAttributes="Bold"
                               TextColor="{StaticResource TextSecondary}"
                               BackgroundColor="{StaticResource Background}"
                               Padding="12,6,12,2" />
                    </DataTemplate>
                </CollectionView.GroupHeaderTemplate>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="vm:DeckCardDisplayItem">
                        <SwipeView>
                            <SwipeView.RightItems>
                                <SwipeItems>
                                    <SwipeItem Text="Remove"
                                               BackgroundColor="#B00020"
                                               Command="{Binding Source={RelativeSource AncestorType={x:Type pages:DeckDetailPage}}, Path=BindingContext.RemoveCardCommand}"
                                               CommandParameter="{Binding .}" />
                                </SwipeItems>
                            </SwipeView.RightItems>
                            <Border Style="{StaticResource CardStyle}"
                                    Margin="8,4">
                                <Grid ColumnDefinitions="Auto,*,Auto,Auto,Auto"
                                      Padding="8,6"
                                      ColumnSpacing="6">
                                    <Border BackgroundColor="{StaticResource Surface}"
                                            Padding="6,2"
                                            StrokeShape="RoundRectangle 6"
                                            Stroke="Transparent"
                                            VerticalOptions="Center">
                                        <Label Text="{Binding Entity.Quantity}"
                                               TextColor="{StaticResource TextPrimary}"
                                               FontSize="12"
                                               FontAttributes="Bold" />
                                    </Border>
                                    <Label Grid.Column="1"
                                           Text="{Binding DisplayName}"
                                           TextColor="{StaticResource TextPrimary}"
                                           FontSize="14"
                                           VerticalOptions="Center"
                                           LineBreakMode="TailTruncation" />
                                    <controls:ManaCostView Grid.Column="2"
                                           ManaText="{Binding ManaCostText}"
                                           SymbolSize="11"
                                           VerticalOptions="Center" />
                                    <Button Grid.Column="3"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type pages:DeckDetailPage}}, Path=BindingContext.DecrementCardCommand}"
                                            CommandParameter="{Binding .}"
                                            BackgroundColor="{StaticResource Surface}"
                                            TextColor="{StaticResource TextPrimary}"
                                            WidthRequest="44"
                                            HeightRequest="44"
                                            Padding="0"
                                            Margin="2,0"
                                            FontSize="18"
                                            CornerRadius="4">
                                        <Button.ImageSource>
                                            <FontImageSource FontFamily="FASolid"
                                                             Glyph="{x:Static fa:Solid.Minus}"
                                                             Size="16"
                                                             Color="{StaticResource TextPrimary}" />
                                        </Button.ImageSource>
                                    </Button>
                                    <Button Grid.Column="4"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type pages:DeckDetailPage}}, Path=BindingContext.IncrementCardCommand}"
                                            CommandParameter="{Binding .}"
                                            BackgroundColor="{StaticResource Accent}"
                                            TextColor="Black"
                                            WidthRequest="44"
                                            HeightRequest="44"
                                            Padding="0"
                                            Margin="2,0"
                                            FontSize="18"
                                            CornerRadius="4">
                                        <Button.ImageSource>
                                            <FontImageSource FontFamily="FASolid"
                                                             Glyph="{x:Static fa:Solid.Plus}"
                                                             Size="16"
                                                             Color="Black" />
                                        </Button.ImageSource>
                                    </Button>
                                </Grid>
                            </Border>
                        </SwipeView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- Sideboard Tab -->
            <CollectionView IsVisible="{Binding IsSideboardTab}"
                            ItemsSource="{Binding SideboardCards}"
                            SelectionMode="None">
                <CollectionView.Header>
                    <Label Text="Sideboard"
                           FontSize="12"
                           FontAttributes="Bold"
                           TextColor="{StaticResource TextSecondary}"
                           Padding="12,8,12,4" />
                </CollectionView.Header>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="vm:DeckCardDisplayItem">
                        <SwipeView>
                            <SwipeView.RightItems>
                                <SwipeItems>
                                    <SwipeItem Text="Remove"
                                               BackgroundColor="#B00020"
                                               Command="{Binding Source={RelativeSource AncestorType={x:Type pages:DeckDetailPage}}, Path=BindingContext.RemoveCardCommand}"
                                               CommandParameter="{Binding .}" />
                                </SwipeItems>
                            </SwipeView.RightItems>
                            <Border Style="{StaticResource CardStyle}"
                                    Margin="8,4">
                                <Grid ColumnDefinitions="Auto,*,Auto,Auto,Auto"
                                      Padding="8,6"
                                      ColumnSpacing="6">
                                    <Border BackgroundColor="{StaticResource Surface}"
                                            Padding="6,2"
                                            StrokeShape="RoundRectangle 6"
                                            Stroke="Transparent"
                                            VerticalOptions="Center">
                                        <Label Text="{Binding Entity.Quantity}"
                                               TextColor="{StaticResource TextPrimary}"
                                               FontSize="12"
                                               FontAttributes="Bold" />
                                    </Border>
                                    <Label Grid.Column="1"
                                           Text="{Binding DisplayName}"
                                           TextColor="{StaticResource TextPrimary}"
                                           FontSize="14"
                                           VerticalOptions="Center"
                                           LineBreakMode="TailTruncation" />
                                    <controls:ManaCostView Grid.Column="2"
                                           ManaText="{Binding ManaCostText}"
                                           SymbolSize="11"
                                           VerticalOptions="Center" />
                                    <Button Grid.Column="3"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type pages:DeckDetailPage}}, Path=BindingContext.DecrementCardCommand}"
                                            CommandParameter="{Binding .}"
                                            BackgroundColor="{StaticResource Surface}"
                                            TextColor="{StaticResource TextPrimary}"
                                            WidthRequest="44"
                                            HeightRequest="44"
                                            Padding="0"
                                            Margin="2,0"
                                            FontSize="18"
                                            CornerRadius="4">
                                        <Button.ImageSource>
                                            <FontImageSource FontFamily="FASolid"
                                                             Glyph="{x:Static fa:Solid.Minus}"
                                                             Size="16"
                                                             Color="{StaticResource TextPrimary}" />
                                        </Button.ImageSource>
                                    </Button>
                                    <Button Grid.Column="4"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type pages:DeckDetailPage}}, Path=BindingContext.IncrementCardCommand}"
                                            CommandParameter="{Binding .}"
                                            BackgroundColor="{StaticResource Accent}"
                                            TextColor="Black"
                                            WidthRequest="44"
                                            HeightRequest="44"
                                            Padding="0"
                                            Margin="2,0"
                                            FontSize="18"
                                            CornerRadius="4">
                                        <Button.ImageSource>
                                            <FontImageSource FontFamily="FASolid"
                                                             Glyph="{x:Static fa:Solid.Plus}"
                                                             Size="16"
                                                             Color="Black" />
                                        </Button.ImageSource>
                                    </Button>
                                </Grid>
                            </Border>
                        </SwipeView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- Stats Tab -->
            <ScrollView IsVisible="{Binding IsStatsTab}" Padding="16,8">
                <VerticalStackLayout Spacing="16">

                    <!-- Mana Curve Chart -->
                    <VerticalStackLayout Spacing="4">
                        <Label Text="Mana Curve"
                               FontSize="13"
                               FontAttributes="Bold"
                               TextColor="{StaticResource TextSecondary}" />
                        <controls:ManaCurveView ManaCurve="{Binding Stats.ManaCurve}"
                                                HeightRequest="140"
                                                BackgroundColor="Transparent" />
                    </VerticalStackLayout>

                    <!-- Stats Grid -->
                    <VerticalStackLayout Spacing="4">
                        <Label Text="Composition"
                               FontSize="13"
                               FontAttributes="Bold"
                               TextColor="{StaticResource TextSecondary}" />
                        <Grid ColumnDefinitions="*,*,*" RowDefinitions="Auto,Auto,Auto"
                              RowSpacing="8" ColumnSpacing="8">
                            <Border Style="{StaticResource CardStyle}" Grid.Row="0" Grid.Column="0">
                                <VerticalStackLayout HorizontalOptions="Center">
                                    <Label Text="{Binding Stats.TotalCards}" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" TextColor="{StaticResource Accent}" />
                                    <Label Text="Total" FontSize="11" TextColor="{StaticResource TextSecondary}" HorizontalOptions="Center" />
                                </VerticalStackLayout>
                            </Border>
                            <Border Style="{StaticResource CardStyle}" Grid.Row="0" Grid.Column="1">
                                <VerticalStackLayout HorizontalOptions="Center">
                                    <Label Text="{Binding Stats.AvgCMC, StringFormat='{0:F1}'}" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" TextColor="{StaticResource Accent}" />
                                    <Label Text="Avg CMC" FontSize="11" TextColor="{StaticResource TextSecondary}" HorizontalOptions="Center" />
                                </VerticalStackLayout>
                            </Border>
                            <Border Style="{StaticResource CardStyle}" Grid.Row="0" Grid.Column="2">
                                <VerticalStackLayout HorizontalOptions="Center">
                                    <Label Text="{Binding Stats.Lands}" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" TextColor="{StaticResource Accent}" />
                                    <Label Text="Lands" FontSize="11" TextColor="{StaticResource TextSecondary}" HorizontalOptions="Center" />
                                </VerticalStackLayout>
                            </Border>
                            <Border Style="{StaticResource CardStyle}" Grid.Row="1" Grid.Column="0">
                                <VerticalStackLayout HorizontalOptions="Center">
                                    <Label Text="{Binding Stats.Creatures}" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" TextColor="{StaticResource TextPrimary}" />
                                    <Label Text="Creatures" FontSize="11" TextColor="{StaticResource TextSecondary}" HorizontalOptions="Center" />
                                </VerticalStackLayout>
                            </Border>
                            <Border Style="{StaticResource CardStyle}" Grid.Row="1" Grid.Column="1">
                                <VerticalStackLayout HorizontalOptions="Center">
                                    <Label Text="{Binding Stats.Instants}" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" TextColor="{StaticResource TextPrimary}" />
                                    <Label Text="Instants" FontSize="11" TextColor="{StaticResource TextSecondary}" HorizontalOptions="Center" />
                                </VerticalStackLayout>
                            </Border>
                            <Border Style="{StaticResource CardStyle}" Grid.Row="1" Grid.Column="2">
                                <VerticalStackLayout HorizontalOptions="Center">
                                    <Label Text="{Binding Stats.Sorceries}" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" TextColor="{StaticResource TextPrimary}" />
                                    <Label Text="Sorceries" FontSize="11" TextColor="{StaticResource TextSecondary}" HorizontalOptions="Center" />
                                </VerticalStackLayout>
                            </Border>
                            <Border Style="{StaticResource CardStyle}" Grid.Row="2" Grid.Column="0">
                                <VerticalStackLayout HorizontalOptions="Center">
                                    <Label Text="{Binding Stats.Artifacts}" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" TextColor="{StaticResource TextPrimary}" />
                                    <Label Text="Artifacts" FontSize="11" TextColor="{StaticResource TextSecondary}" HorizontalOptions="Center" />
                                </VerticalStackLayout>
                            </Border>
                            <Border Style="{StaticResource CardStyle}" Grid.Row="2" Grid.Column="1">
                                <VerticalStackLayout HorizontalOptions="Center">
                                    <Label Text="{Binding Stats.Enchantments}" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" TextColor="{StaticResource TextPrimary}" />
                                    <Label Text="Enchants" FontSize="11" TextColor="{StaticResource TextSecondary}" HorizontalOptions="Center" />
                                </VerticalStackLayout>
                            </Border>
                            <Border Style="{StaticResource CardStyle}" Grid.Row="2" Grid.Column="2">
                                <VerticalStackLayout HorizontalOptions="Center">
                                    <Label Text="{Binding Stats.Planeswalkers}" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" TextColor="{StaticResource TextPrimary}" />
                                    <Label Text="Planeswalkers" FontSize="11" TextColor="{StaticResource TextSecondary}" HorizontalOptions="Center" />
                                </VerticalStackLayout>
                            </Border>
                        </Grid>
                    </VerticalStackLayout>

                    <!-- Bulk land helper -->
                    <Button Text="Auto-suggest lands"
                            Command="{Binding SuggestLandsCommand}"
                            BackgroundColor="{StaticResource Accent}"
                            TextColor="Black"
                            FontSize="13"
                            HeightRequest="40"
                            CornerRadius="20"
                            HorizontalOptions="Center"
                            Margin="0,8,0,0" />

                    <!-- Status message -->
                    <Label Text="{Binding StatusDisplayText}"
                           FontSize="12"
                           TextColor="{Binding StatusTextColor}"
                           HorizontalOptions="Center" />

                </VerticalStackLayout>
            </ScrollView>

            <!-- FAB: Add Card (visible on Commander, Main, Sideboard tabs) -->
            <Button x:Name="AddCardFab"
                    Clicked="OnAddCardClicked"
                    IsVisible="{Binding IsStatsTab, Converter={StaticResource InvertBool}}"
                    Style="{StaticResource FloatingActionButtonStyle}"
                    ZIndex="10">
                <Button.ImageSource>
                    <FontImageSource FontFamily="FASolid"
                                     Glyph="{x:Static fa:Solid.Plus}"
                                     Size="20"
                                     Color="White" />
                </Button.ImageSource>
            </Button>

        </Grid>

        <!-- Row 4: Footer -->
        <Grid Grid.Row="4"
              Padding="12,8"
              BackgroundColor="{StaticResource Surface}">
            <Label Text="{Binding StatusDisplayText}"
                   FontSize="12"
                   TextColor="{Binding StatusTextColor}"
                   HorizontalOptions="Center"
                   VerticalOptions="Center" />
        </Grid>

    </Grid>

    <ContentPage.Resources>
        <ResourceDictionary>
            <toolkit:InvertedBoolConverter x:Key="InvertBool" />
        </ResourceDictionary>
    </ContentPage.Resources>

</ContentPage>
