<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
             x:Class="AetherVault.Pages.SearchFiltersPage"
             Title="Search Filters"
             BackgroundColor="{StaticResource Background}"
             Shell.PresentationMode="ModalAnimated">

    <ContentPage.Resources>
        <ResourceDictionary>
            <OnIdiom x:Key="HeaderFontSize" x:TypeArguments="x:Double" Phone="16" Tablet="20" Desktop="20" />
            <OnIdiom x:Key="BodyFontSize" x:TypeArguments="x:Double" Phone="14" Tablet="18" Desktop="18" />
            <OnIdiom x:Key="SmallFontSize" x:TypeArguments="x:Double" Phone="12" Tablet="16" Desktop="16" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="*,Auto">

        <ScrollView Grid.Row="0" Padding="16">
            <VerticalStackLayout Spacing="16">

                <!-- Colors -->
                <VerticalStackLayout Spacing="8">
                    <Label Text="Colors" FontSize="{StaticResource HeaderFontSize}" FontAttributes="Bold" />
                    <FlexLayout x:Name="ColorButtons" Wrap="NoWrap" JustifyContent="Start" />
                </VerticalStackLayout>

                <!-- Card Text / Keywords -->
                <VerticalStackLayout Spacing="0">
                    <Label Text="Card Text" Style="{StaticResource FormInputLabel}" />
                    <Border Style="{StaticResource FormInputContainer}">
                        <Entry x:Name="KeywordsEntry" Placeholder="Card Text" BackgroundColor="Transparent" TextColor="{StaticResource TextPrimary}" />
                    </Border>
                </VerticalStackLayout>

                <!-- Card Type -->
                <VerticalStackLayout Spacing="0">
                    <Label Text="Card Type" Style="{StaticResource FormInputLabel}" />
                    <Border Style="{StaticResource FormInputContainer}">
                        <Picker x:Name="TypePicker" Title="Card Type" TextColor="{StaticResource TextPrimary}" BackgroundColor="Transparent">
                            <Picker.ItemsSource>
                                <x:Array Type="{x:Type x:String}">
                            <x:String>Any</x:String>
                            <x:String>Artifact</x:String>
                            <x:String>Battle</x:String>
                            <x:String>Creature</x:String>
                            <x:String>Enchantment</x:String>
                            <x:String>Instant</x:String>
                            <x:String>Land</x:String>
                            <x:String>Planeswalker</x:String>
                            <x:String>Sorcery</x:String>
                            <x:String>Kindred</x:String>
                                </x:Array>
                            </Picker.ItemsSource>
                        </Picker>
                    </Border>
                </VerticalStackLayout>

                <!-- Subtype -->
                <VerticalStackLayout Spacing="0">
                    <Label Text="Subtype" Style="{StaticResource FormInputLabel}" />
                    <Border Style="{StaticResource FormInputContainer}">
                        <Entry x:Name="SubtypeEntry" Placeholder="Subtype" BackgroundColor="Transparent" TextColor="{StaticResource TextPrimary}" />
                    </Border>
                </VerticalStackLayout>

                <!-- Supertype -->
                <VerticalStackLayout Spacing="0">
                    <Label Text="Supertype" Style="{StaticResource FormInputLabel}" />
                    <Border Style="{StaticResource FormInputContainer}">
                        <Entry x:Name="SupertypeEntry" Placeholder="Supertype" BackgroundColor="Transparent" TextColor="{StaticResource TextPrimary}" />
                    </Border>
                </VerticalStackLayout>

                <!-- Rarity -->
                <VerticalStackLayout Spacing="4">
                    <Label Text="Rarity" FontSize="{StaticResource BodyFontSize}" />
                    <FlexLayout Wrap="Wrap" JustifyContent="Start">
                        <material:CheckBox x:Name="ChkCommon" Text="Common" Color="{StaticResource Primary}" Margin="0,0,12,4" />
                        <material:CheckBox x:Name="ChkUncommon" Text="Uncommon" Color="{StaticResource Primary}" Margin="0,0,12,4" />
                        <material:CheckBox x:Name="ChkRare" Text="Rare" Color="{StaticResource Primary}" Margin="0,0,12,4" />
                        <material:CheckBox x:Name="ChkMythic" Text="Mythic" Color="{StaticResource Primary}" Margin="0,0,12,4" />
                    </FlexLayout>
                </VerticalStackLayout>

                <!-- CMC Range -->
                <VerticalStackLayout Spacing="4">
                    <Label Text="Mana Value (CMC)" FontSize="{StaticResource BodyFontSize}" />
                    <Grid ColumnDefinitions="*,Auto,*" ColumnSpacing="8">
                        <VerticalStackLayout>
                            <Label x:Name="CMCMinLabel" Text="Min: 0" FontSize="{StaticResource SmallFontSize}"
                                   TextColor="{StaticResource TextSecondary}" />
                            <Slider x:Name="CMCMinSlider" Minimum="0" Maximum="16"
                                    ValueChanged="OnCMCChanged"
                                    ThumbColor="{StaticResource Primary}"
                                    MinimumTrackColor="{StaticResource Primary}" />
                        </VerticalStackLayout>
                        <Label Grid.Column="1" Text="to" VerticalOptions="End" Margin="0,0,0,8" FontSize="{StaticResource BodyFontSize}" />
                        <VerticalStackLayout Grid.Column="2">
                            <Label x:Name="CMCMaxLabel" Text="Max: 16+" FontSize="{StaticResource SmallFontSize}"
                                   TextColor="{StaticResource TextSecondary}" />
                            <Slider x:Name="CMCMaxSlider" Minimum="0" Maximum="16" Value="16"
                                    ValueChanged="OnCMCChanged"
                                    ThumbColor="{StaticResource Primary}"
                                    MinimumTrackColor="{StaticResource Primary}" />
                        </VerticalStackLayout>
                    </Grid>
                </VerticalStackLayout>

                <!-- Power / Toughness -->
                <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                    <VerticalStackLayout Spacing="0">
                    <Label Text="Power" Style="{StaticResource FormInputLabel}" />
                    <Border Style="{StaticResource FormInputContainer}">
                        <Entry x:Name="PowerEntry" Placeholder="Power" BackgroundColor="Transparent" TextColor="{StaticResource TextPrimary}" Keyboard="Numeric" />
                    </Border>
                </VerticalStackLayout>
                    <VerticalStackLayout Spacing="0" Grid.Column="1">
                    <Label Text="Toughness" Style="{StaticResource FormInputLabel}" />
                    <Border Style="{StaticResource FormInputContainer}">
                        <Entry x:Name="ToughnessEntry" Placeholder="Toughness" BackgroundColor="Transparent" TextColor="{StaticResource TextPrimary}" Keyboard="Numeric" />
                    </Border>
                </VerticalStackLayout>
                </Grid>

                <!-- Format Legality -->
                <VerticalStackLayout Spacing="0">
                    <Label Text="Format" Style="{StaticResource FormInputLabel}" />
                    <Border Style="{StaticResource FormInputContainer}">
                        <Picker x:Name="FormatPicker" Title="Format" TextColor="{StaticResource TextPrimary}" BackgroundColor="Transparent" />
                    </Border>
                </VerticalStackLayout> />

                <!-- Set Code -->
                <VerticalStackLayout Spacing="0">
                    <Label Text="Set Code" Style="{StaticResource FormInputLabel}" />
                    <Border Style="{StaticResource FormInputContainer}">
                        <Entry x:Name="SetEntry" Placeholder="Set Code" BackgroundColor="Transparent" TextColor="{StaticResource TextPrimary}" />
                    </Border>
                </VerticalStackLayout>

                <!-- Artist -->
                <VerticalStackLayout Spacing="0">
                    <Label Text="Artist" Style="{StaticResource FormInputLabel}" />
                    <Border Style="{StaticResource FormInputContainer}">
                        <Entry x:Name="ArtistEntry" Placeholder="Artist" BackgroundColor="Transparent" TextColor="{StaticResource TextPrimary}" />
                    </Border>
                </VerticalStackLayout>

                <!-- Special Options -->
                <VerticalStackLayout Spacing="4">
                    <material:CheckBox x:Name="ChkPrimarySide" Text="Primary side only" IsChecked="True" Color="{StaticResource Primary}" Margin="0,0,0,8" />
                    <material:CheckBox x:Name="ChkNoVariations" Text="Exclude variations" Color="{StaticResource Primary}" Margin="0,0,0,8" />
                </VerticalStackLayout>

            </VerticalStackLayout>
        </ScrollView>

        <!-- Bottom buttons -->
        <Grid Grid.Row="1"
              Padding="16,8"
              ColumnDefinitions="*,*,*"
              ColumnSpacing="8"
              BackgroundColor="{StaticResource Surface}">
            <Button Text="Reset"
                    Clicked="OnResetClicked"
                    BackgroundColor="{StaticResource CardBackground}" />
            <Button Grid.Column="1"
                    Text="Cancel"
                    Clicked="OnCancelClicked"
                    BackgroundColor="{StaticResource CardBackground}" />
            <Button Grid.Column="2"
                    Text="Apply"
                    Clicked="OnApplyClicked" />
        </Grid>

    </Grid>

</ContentPage>